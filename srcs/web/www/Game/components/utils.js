export function setProperties(game, life, round, gameMode){
	game.lifeNumber = life;
	game.roundNumber = round;
	game.gameMode = gameMode;
}

export function setPlayerName(game, name){
	game.playerName = name
}